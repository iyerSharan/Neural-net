Some insight:
Uses: tf

Here is an implementation of a classical NN to get an overview of what happens in the background of training a NN. It
includes Forward pass, backward pass, activation layers and an example datasset is used to train the model to predict the
pattern. Later the MNIST dataset is trained and test to determine the handwritten text :).